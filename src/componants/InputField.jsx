import React from 'react';


export default class InputField extends React.Componant{

    constructor(props){
        super(props);
        // state is set here 
        this.state = {
            buttontoggle : 0,
            buyfield : 0, 
            askfield : 0
        };
    }
    
    render(
        // this needs to render all three componants: submit button, ask and buy fields. 
    );


} 